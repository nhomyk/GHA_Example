name: Cypress Tests

on: [push, pull_request]

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        spec-file: [1.cy.js]

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22.2.0' # Specify the Node.js version you need


      - name: Run Cypress tests
        run: npx cypress run --spec “/Users/nicholashomyk/Documents/GHA_example/Cypress/e2e/tests/${{ matrix.spec-file }}"
